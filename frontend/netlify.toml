[build]
  base = "frontend"
  # 'npm ci' is cleaner than 'npm install' for servers
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # This tells npm to ignore the existing node_modules
  NPM_FLAGS = "--include=dev"
