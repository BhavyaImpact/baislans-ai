[build]
  base = "frontend"
  # This deletes the broken folder and reinstalls everything fresh
  command = "rm -rf node_modules && npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
